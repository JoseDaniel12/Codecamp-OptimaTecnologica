const estados = {
    // Estados de Usuarios y Productos
    ACTIVO: 'Activo',
    INACTIVO: 'Inactivo',

    // Estados de ordens
    CONFIRMADO: 'Confirmado',
    ENTREGADO: 'Entregado',
    RECHAZADO: 'Rechazado',
};

module.exports = estados;